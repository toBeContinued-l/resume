# 文件解析模块任务

## 最小可执行任务

- [x] 定义 `ParsedResumeDocument`、`ParsedBlock`、`ParsedTable`、`ParsedAsset` 和 `ParserWarning` 类型。
- [x] 实现文件类型识别，区分 `.doc`、`.docx` 和 `.pdf`。
- [x] 实现 `.doc` 解析入口：调用 LibreOffice headless 转换为 `.docx`。
- [x] 实现 `.doc` 转换失败时的错误记录和任务失败处理。
- [x] 实现 `.docx` 解析：提取纯文本。
- [x] 实现 `.docx` 解析：提取语义化 HTML。
- [x] 实现 `.docx` 解析：提取标题、段落、列表、链接和常见富文本标记。
- [x] 实现 `.docx` 解析：提取表格结构。
- [x] 实现 `.docx` 解析：提取可保留图片资源并写入临时 `assets/`。
- [x] 实现文本型 PDF 解析：提取页码、文本块、坐标和顺序。
- [x] 实现扫描版或纯图片 PDF 判断：有效文本为空或低于阈值时返回无有效内容。
- [x] 实现解析结果有效性校验，空文本返回可重试错误。
- [x] 实现解析警告生成：样式损失、PDF 顺序不确定、低文本置信度等。
- [x] 确保复杂样式不作为事实内容进入 AI 输入。
- [x] 准备解析测试样本：普通 `.docx`、表格 `.docx`、图片 `.docx`、旧版 `.doc`、文本 PDF、扫描版 PDF、空文件、损坏文件。
- [x] 编写 `.docx` 解析测试。
- [x] 编写文本 PDF 解析测试。
- [x] 编写扫描版 PDF 无 OCR 测试。
- [x] 编写解析空内容可重试错误测试。

## 完成标准

- [x] 三类文件都有明确解析入口和失败处理。
- [x] 解析结果可直接作为 AI 模块输入。
- [x] 不支持 OCR 的边界被明确执行。
