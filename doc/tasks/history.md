# 历史记录模块任务

## 最小可执行任务

- [x] 创建历史记录页面 `/dashboard` 或项目约定的等价路由。
- [x] 实现历史记录 API `/api/resumes`。
- [x] 为历史记录 API 添加登录态校验。
- [x] 查询时只返回当前用户的未删除简历记录。
- [x] 展示简历标题。
- [x] 展示创建时间。
- [x] 展示最近更新时间。
- [x] 展示在线链接。
- [x] 展示编辑入口。
- [x] 展示当前用户剩余可上传数量。
- [x] 实现删除 API `/api/resumes/{resumeId}`。
- [x] 为删除 API 添加登录态和简历所有权校验。
- [x] 删除时将 `resumes.status` 更新为 `deleted`。
- [x] 删除时将 `resumes.is_deleted` 更新为 `true`，作为明确软删除标识。
- [x] 删除时写入 `deleted_at`。
- [x] 删除时将对应在线链接置为失效。
- [x] 删除后历史记录列表不再展示该记录。
- [x] 删除后上传数量限制重新计算，允许用户继续上传到 3 份上限内。
- [x] 确保首期不提供版本回退入口。
- [x] 编写历史记录所有权过滤测试。
- [x] 编写已删除记录不展示测试。
- [x] 编写删除记录后链接失效测试。
- [x] 编写删除后可重新上传测试。

## 完成标准

- [x] 用户只能看到、编辑和删除自己的简历记录。
- [x] 软删除后记录不再展示，链接立即失效。
- [x] 历史记录准确参与 3 份上传限制。
