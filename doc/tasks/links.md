# 在线链接模块任务

## 最小可执行任务

- [x] 定义链接访问模式枚举：`public`、`private_link`、`password`。
- [x] 实现随机 slug 生成，随机强度不低于 128 bit。
- [x] 实现 slug 唯一性校验和冲突重试。
- [x] 实现链接配置读取 API `/api/resumes/{resumeId}/link`。
- [x] 实现链接配置更新 API `/api/resumes/{resumeId}/link`。
- [x] 为链接配置 API 添加登录态和简历所有权校验。
- [x] 实现首次保存时创建 `resume_links` 记录。
- [x] 实现再次保存时更新已有链接配置。
- [x] 实现公开访问模式保存。
- [x] 实现私密链接访问模式保存。
- [x] 实现密码访问模式保存，访问密码只保存哈希。
- [x] 实现从密码访问切换到其他模式时清空密码哈希。
- [x] 实现删除简历时将 `resume_links.is_active` 更新为 `false`。
- [x] 实现密码校验 API `/api/public-links/{slug}/verify-password`。
- [x] 确保密码访问不设置访问会话缓存。
- [x] 确保密码错误时不返回任何简历内容。
- [x] 编写 slug 随机性和唯一性测试。
- [x] 编写访问密码哈希测试。
- [x] 编写访问模式切换测试。
- [x] 编写删除简历后链接失效测试。

## 完成标准

- [x] 保存简历后能生成稳定在线链接。
- [x] 三种访问模式都能被配置和持久化。
- [x] 密码访问模式不会明文保存密码，也不会缓存访问会话。
